<% if current_user.nil? %>
<div class="container text-center m-auto ">
    <h2 class="m-auto">connectes-toi</h2>
</div>
<% else %>
    <div class="container align-items-center">
        <h2 class="my-2 text-center">Liste des Participants par entrainement</h2>
        <h4><strong>Séance du <%= @training.date.time.to_formatted_s(:short)%> - (Nombres d'inscrits = <%= @participants.size %>)</strong></h4>
        <ul>
            <% @participants.each do |p| %>
                <li><%= p.user.nom %> <%= p.user.prenom %> - <%= p.user.phone %></li> 
            <% end %>
        </ul>

        <%= link_to "Exporter en CSV", training_path, class: "btn btn-dark" %>
    </div>
<% end %>

